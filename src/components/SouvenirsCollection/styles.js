import styled from "styled-components";
import * as GridComponent from "../Grid";

export const SouvenirsCollection = styled.div``;

export const Empty = styled.p``;

export const Grid = styled(GridComponent.Grid)``;

export const GridItem = styled(GridComponent.GridItem)``;